apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: planguru-__PLANGURU_ENV__-api
  name: planguru-__PLANGURU_ENV__-api
  namespace: default
spec:
  selector:
    matchLabels:
      app: planguru-__PLANGURU_ENV__-api
  strategy:
    rollingUpdate:
      maxSurge: __MAX_SURGE__
      maxUnavailable: __MAX_UNAVAILABLE__
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: planguru-__PLANGURU_ENV__-api
    spec:
      containers:
        - env:
            - name: PLANGURU_ENV
              value: __PLANGURU_ENV__
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: openai-key
                  key: OPENAI_API_KEY
          image: gcr.io/planguru-cloud/planguru-api-__PLANGURU_ENV__:__REVISION_ID__
          imagePullPolicy: Always
          name: planguru-cloud-sha256
          readinessProbe:
            failureThreshold: 1
            exec:
              command:
                - curl
                - http://localhost:3000/api/home
            initialDelaySeconds: 5
            periodSeconds: 3
            successThreshold: 1
            timeoutSeconds: 5
          livenessProbe:
            failureThreshold: 5
            exec:
              command:
                - curl
                - http://localhost:3000/api/home
            initialDelaySeconds: 30
            periodSeconds: 5
            successThreshold: 1
            timeoutSeconds: 15
          resources:
            requests:
              cpu: 1000m
              memory: 256Mi
          securityContext:
            privileged: true
            capabilities:
              add:
                - SYS_ADMIN
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      terminationGracePeriodSeconds: 15
